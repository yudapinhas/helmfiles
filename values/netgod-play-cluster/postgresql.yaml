auth:
  postgresPassword: "postgres"

primary:
  securityContext:
    enabled: false

  volumePermissions:
    enabled: true

  initdb:
    scripts:
      init-etldb.sql: |
        CREATE DATABASE etldb;